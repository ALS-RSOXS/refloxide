# Example: material parameterizations

This page will demonstrate how to define material optics using supported
parameterizations and convert them into the canonical internal
representation used by the solver.

Target parameterizations:

- $n + ik$
- $1 - \delta + i\beta$
- $\chi$
- $\varepsilon$
- SLD and scattering-factor based inputs where supported
